---
name: threadfold
description: Inspect and revise RUVORA ThreadFold local consolidation previews with immutable source provenance. Production Hub integration is unavailable.
---

Use the ThreadFold MCP tools to inspect capabilities before preparing a plan. Production mode fails closed; a fixture must be explicitly configured by the operator. Explain protected targets, source coverage, conflicts, exclusions and expected effects before any review acknowledgement. Review text is not approval. Never manufacture receipts, request signing keys, call native archive APIs, edit Hub registries, or write messages to source threads. No approval issuance tool exists. Local `demo` is an explicitly labeled fixture simulation, not live user confirmation. Read README.md and docs/USAGE_KO.md for setup and data recovery boundaries.
